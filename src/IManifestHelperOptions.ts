namespace Manifold {
    export interface IManifestHelperOptions {
        manifest: Manifesto.IManifest;
        licenseMap: Object;
        sequenceIndex: number;
        canvasIndex: number;
    }
}